version: '3'
services:
#  db:
#    build:
#      context: .
#      dockerfile: deploy/db/Dockerfile
#    volumes:
#      - postgres_db:/var/lib/postgresql/data
#    env_file:
#      - .env
  web:
    build:
      context: .
      dockerfile: deploy/web/Dockerfile
    volumes:
      - sqlite_db:/db
    ports:
      - "80:80"

volumes:
  sqlite_db:
#  static:
#  postgres_db: